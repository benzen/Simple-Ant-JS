extends layout
block append scripts
    script(src='/script/gameStatus', type="text/coffeescript")
    script
        require(["cs!/script/gameStatus.coffee"],function(){
            renderGameStatus( !{gameStatus});
        });
    
block content
    canvas( width="800", height="800") Your browser doesn't support html canvas    