extends layout
block append scripts
    script 
      require(['cs!/script/gameStatus'],function(gameStatus){
        $().ready(function(){
          var gameId =  $(location).attr('href').match(/game\\/(\\d)\\/status/)[1]
          $.getJSON("/game/"+gameId+"/status.json",null, gameStatus.renderGameStatus)
        });
      });
    
block content
    canvas( width="800", height="800") Your browser doesn't support html canvas